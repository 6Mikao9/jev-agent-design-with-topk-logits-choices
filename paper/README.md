# Paper draft

Compile with `pdflatex -interaction=nonstopmode main.tex` twice from this directory (or use `latexmk -pdf main.tex`). The evaluation table and all numerical results are intentionally TODO placeholders. Fill them in the Evaluation plan section after running the planned option-scale, resident-K, backend-capability, recovery, cost, latency, thrashing, and semantic-freshness experiments. This repository currently contains only a mechanism baseline and small Jev replay; no PDF or large results are committed.
